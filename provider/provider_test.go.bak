package provider

import (
	"fmt"
	"testing"
)

func Test_Lun(t *testing.T) {
	blockNameSeq := []string{"azurerm_container_group"}
	schemaMap := GetArgSchema(blockNameSeq, "resource")
	fields := GetDocSortedArgNames(schemaMap)
	for _, field := range fields {
		fmt.Println(field)
		fmt.Println(schemaMap[field].Required)
	}
}
